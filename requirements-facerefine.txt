# Optional FaceRefine dependencies. Install using ComfyUI's Python environment.
ultralytics
scipy
scenedetect>=0.7
# Main U11 identity-reference tracking requires these; positional standalone previews do not.
insightface
onnxruntime
