# U11 61s implementation plan

> Execution: subagent-driven-development for independent planner/UI tasks; primary owns integration and runtime. User approved design 2026-09-07. Work on feat/u11-longfilm-61s, preserving existing uncommitted FHD changes.

Goal: executable, resumable 1464-frame FL2VA AV continuation through the existing U11 installation, with editable timed prompt plan.
Architecture: isolated long-film output node runs registered Infinite V14 nodes sequentially, existing U11 acceleration/decoder/SR, atomic disk checkpoints, frame-exact media join. UI has a dedicated long-film node/panel linked from the existing director rather than passing 61 to its single-clip executor.

- [ ] Planner: nodes/longfilm_plan.py + tests/test_longfilm_plan.py. API validate_story(dict|string), plan_chunks(story,total_vram_gb), compile_prompt(story,chunk). Validate 1464 frames, continuous timed beats, dialogue intervals, Base-only assets. AV cut counts must be 39+51n for each source endpoint, so actual sampling chunks may cross editorial card boundaries. Final segment may trim only end padding. Keep exact global-to-local timestamps including 39-frame continuation head.
- [ ] Runtime: nodes/longfilm.py plus nodes/longfilm_media.py and tests/test_longfilm.py. Load registered Infinite V14 classes, capability check. Sequential node executes existing single-clip director policy for grid/LoRA, captures pass-one x0 before upscale, decodes raw context for safe handover, stores safetensors without unsafe pickle. Content fingerprints include model names/config, timing, prefix context; saved segment and latent hashes verified. Re-render export on only SR/target change. Never concatenate all RGB frames in memory.
- [ ] Interface: js/minimax_h3_longfilm.js + tests/test_longfilm_ui.py + templates/u11_longfilm_story.json. Dedicated MiniMaxH3LongFilm61 output node inputs model/clip/video_vae/audio_vae/project_name/story_json/target/seed. Per-card editor and localized labels; load/save structured plan. Stop/resume via own project endpoints; no global interrupt. Unsupported voice/mode clearly rejected.
- [ ] Integration: register class in __init__.py and project status/cancel routes in api/routes.py; derive separate U11-61s workflow using existing local loader names and original U11 assets. Add existing-director link that opens/adds long-film workflow entry without breaking its native queue.
- [ ] Verification: tests fail before new behavior, all regression tests, real node loader/capability smoke, frame/sample-count media fixture, actual GPU short-chain then full generation if environment permits. Preserve concrete failures and never claim unrun GPU quality. Backup then hash-check synced modified files, install pinned Infinite plugin dependency as approved by design.
- [ ] Review: spec and code review, fix correctness issues; document actual supported hardware/targets, instructions and prompt example.
