# 验证记录

环境：`D:\ComfyUI_windows_portable-G313\python_embeded\python.exe`，独立 ComfyUI CPU 实例 `127.0.0.1:8190`。

- 插件单元测试：`73 passed`。
- Python `compileall`：通过。
- JavaScript `node --check js/minimax_h3_director_plus.js`：通过。
- U11 结构校验：32 个主节点、23 条主连线、1 个子图、0 个可见节点重叠。
- 普通/API JSON 解析：通过；U10 原文件未改写。
- `/object_info`：7 个 Plus 节点均已注册。
- `GET /h3-director-plus/schema`：HTTP 200，公开字段均有中文名称。
- `GET /h3-director-plus/status`：HTTP 200；FL2VA/REF2VA、视频/音频 VAE、LightX2V、GGUF、Sage、EasyCache、SolAttn、RIFE 等本机能力可探测；Fish 节点存在但模型未就绪。
- `/h3-director-plus/validate` T2VA：`valid=true`，后端 `fl2va_model`，无节点错误。
- `/h3-director-plus/validate` I2VA + H3 原生音色参考：`valid=true`，后端 `ref2va_model`，无节点错误。
- `/h3-director-plus/validate` Fish S2 缺少目标对白：入队前 HTTP 400，返回中文错误。
- Tensor 形态的 ComfyUI `IMAGE` 输入：通过，不进行多元素布尔转换。

未执行真实生成烟雾测试：CPU 验证实例不加载 15-34 GB H3 模型，避免占用用户正在使用的 GPU/队列。应在用户确认素材和显存窗口后，在 8188 上执行低分辨率真实生成。
